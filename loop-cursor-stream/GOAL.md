# GOAL — cursor-native streaming: show assistant text while Cursor is still writing it, not only on item completion

Target repo: /home/alex/omnigent-fixes (git WORKTREE, branch `trio-v0.10.0-fixes`, HEAD = loop-compact close commit). Mailbox: loop-cursor-stream/. Diagnosis: `loop/REPORT.md` problem 2 + `loop/evidence/iter1/rc-render/CURSOR-NATIVE.md` — cursor-native mirrors Cursor's SQLite `store.db` `blobs` table as COMPLETE items only (`omnigent/cursor_native_forwarder.py`, ~1319 lines: `_read_blob_rows` ~524, high-water rowid, `_post_conversation_item` ~791 → `external_conversation_item`); `CursorNativeExecutor.supports_streaming()` is False (`omnigent/inner/cursor_native_executor.py:41-61`). The claude-native forwarder already has the transient delta primitive: `_post_external_output_text_delta` (`omnigent/claude_native_forwarder.py:4013-4060`) → `response.output_text.delta` SSE, consumed by the UI's rAF-coalesced live-delta path shipped in b0dc3bfaa (`web/src/store/chatStore.ts` `tapLiveDeltas`/`applyLiveDelta`). The user reports Cursor as the slowest-feeling harness precisely because text appears only when the message finishes.

## Mission
Make cursor-native sessions stream assistant text into the UI as transient deltas while Cursor is generating, converging to the same final `external_conversation_item` as today, with no duplicate or reordered text and no change to persisted history.

## Verification floor
Slice 0 produces a MEASURED answer (not a guess) about where partial text is observable; implementation slices add unit tests (fake store rows / fake pane frames) plus one real end-to-end run of the actual `cursor-agent` CLI in a THROWAWAY Omnigent instance (scratch `OMNIGENT_DATA_DIR`/`OMNIGENT_CONFIG_HOME`, port ≥ 17200, torn down) recording: time-to-first-delta vs time-to-complete-item, delta count, and a byte-equality check between concatenated deltas' final text and the completed item. `uv run pytest -q tests/test_cursor_native_forwarder.py` + touched tests green; `cd web && npx vitest run src/store/chatStore.test.ts` green if the store changes; `pre-commit run --files …` clean.

## HARD isolation constraints
As loop-fixes/GOAL.md: never touch ~/.omnigent, ~/.claude/projects, live :6767, `omni host`, the user's :17067 instance / /home/alex/omnigent-fixes-data, or /home/alex/omnigent. Using the user's installed `cursor-agent` CLI (already authenticated in ~/.cursor) from a throwaway workspace dir is ALLOWED and expected for slice 0 and the e2e run — read ~/.cursor store files read-only only; never modify ~/.cursor. Never kill processes you did not start (stop only your own throwaway cursor-agent/tmux). Preserve all commits; new work = `slice(<id>): …` commits with trailer `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`. Rebase-friendly vs upstream #3000/#2702/#4589: additive, env-gated (`OMNIGENT_CURSOR_STREAM=1` default on, `0` = today's behavior). CLAUDE.md rules.

## Slices
0. **probe-partial-text** (investigation, writes only under loop-cursor-stream/evidence/iter1/probe/) — Run a real `cursor-agent` turn (throwaway workspace, long answer prompt e.g. "write 800 words about X") while sampling BOTH candidate sources at ~100 ms: (a) Cursor's `store.db` — does the in-progress assistant blob appear/grow before completion (new rowid early? same rowid updated in place? separate streaming table/meta key? WAL growth?); (b) the tmux pane via `capture-pane -p -e` (the cursor-native pane exists under `/tmp/omnigent-terminal-*/tmux.sock` when launched through Omnigent; for the probe, launching cursor-agent in your own tmux is fine). Record timelines with timestamps. Decide and justify the source: prefer store.db if partial blobs exist (structured, no ANSI); else pane-diff (last-frame diff of the assistant region, ANSI stripped, tolerant of redraw/scroll). Deliver `DECISION.md` with the measured numbers.
1. **cursor-delta-source** — Implement the chosen source in `omnigent/cursor_native_forwarder.py` (or a sibling `cursor_native_stream.py` to keep the forwarder diff small): while a turn is active, poll the source at ~100–150 ms (env-overridable, backs off when idle — reuse the adaptive cadence from b0dc3bfaa), compute the new text suffix for the in-progress assistant message, and POST it via the same transient `response.output_text.delta` shape the claude-native forwarder uses (factor `_post_external_output_text_delta` into a shared helper rather than duplicating; include a stable `messageId` for the in-progress message so the UI's `tapLiveDeltas` path can accumulate it). On completion, the existing complete-item POST must supersede the live preview: the UI must swap the transient block for the persisted item with no flicker/duplicate (check how claude-native does this hand-off in chatStore and reuse it). Set `supports_streaming()` accordingly. Unit tests with fake rows/frames covering: first delta, suffix growth, redraw/scroll (pane source), item completion hand-off, `OMNIGENT_CURSOR_STREAM=0` no-op.
2. **cursor-stream-e2e** — The real cursor-agent run on the throwaway instance with the numbers required by the verification floor, under loop-cursor-stream/evidence/iter1/e2e/. Fix anything it reveals.

## Acceptance
1. DECISION.md exists with measured timelines for both sources and a justified choice.
2. On the throwaway instance a real cursor-native turn shows deltas: time-to-first-delta < 1.5 s after generation starts, ≥ 5 deltas for an 800-word answer, final text byte-equal to the completed item, no duplicated block after completion.
3. Tests green (pytest cursor forwarder + touched, vitest chatStore if touched); Trio-compat command green (`uv run pytest -q tests/tools/builtins/test_spawn.py tests/runner/test_runner_dispatch.py tests/server/integration/test_sessions_child_sessions.py -k 'reasoning_effort or session_create_spawns_child_under_caller or registered_native_agent_create_derives_launch_args_from_root_spec'`); pre-commit clean; `slice(...)` commits present; tree clean except mailbox.
4. Isolation audit: `:6767` 200; `stat -c %Y /home/alex/omnigent-fixes-data/chat.db` unchanged vs loop-cursor-stream/evidence/baseline-testdb-mtime.txt; ~/.cursor unmodified (record `find ~/.cursor -newer loop-cursor-stream/GOAL.md -type f | head` — must list only Cursor's own runtime files, not edits by us); no leftover throwaway processes/listeners.
5. REPORT ends with operator steps: rebuild web, restart :17067 to try it, live deploy/rollback as in loop-fixes/REPORT.md.
