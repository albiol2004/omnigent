# GOAL — cursor model switching must work for every catalogued model, the UI must not display a model that was rejected, and the live preview must not show wrong text mid-stream

Target repo: /home/alex/omnigent-cursor3 (git WORKTREE, branch `cursor-picker-glm`, based on trio-v0.10.0-fixes 15db28694, checked out LIVE in /home/alex/omnigent). Mailbox: loop-cursor3/. Prior loops (context, do not redo): loop-cursor2/ (shipped the render-order + picker-row-match work), loop-cursor-ux/, loop-fork-async/.

## User report after deploying 15db28694 (server restarted 11:13, bundle built 10:59 — the fixes ARE live)
1. **Model switch to GLM 5.2 failed.** The web UI still displays "GLM 5.2" while the CLI pane stayed on Grok. The honest-503 work is functioning (see evidence), so the residual bugs are (a) the picker cannot resolve this model and (b) the UI keeps showing a selection the server rejected.
2. **Live preview is wrong until the stream ends.** Forking a cursor session now renders content (the loop-cursor2 fixes work), but the text shown *during* streaming is wrong; once SSE ends the transcript is perfect.
3. The user prefers `Cursor Grok 4.6` at effort high as the cursor default. **Already handled outside this loop** — `~/.cursor/cli-config.json` was restored to Medium by the coordinator; do not touch it (see isolation).

## Evidence gathered read-only before this loop (do not re-derive; reproduce)
Server log `~/.omnigent/logs/server/server-20260829-111357-588969.log` lines 1123-1142, session `bc02cb1a43d6465fa56bcadf0343b3f7`:
```
Session-change forward rejected ... type='model_change' status=503 body={"error":"cursor_native_model_failed"...}
Model change not applied to the terminal ... model='glm-5.2-high': the runner returned status 503
Internal error: Model change to 'glm-5.2-high' was not applied ... (helpers.py:5175 _surface_model_change_forward_failure)
```
Runner log `runner-bc02cb1a43d6465fa56bcadf0343b3f7-20260829-112116-070369.log`:
```
cursor-native model change failed: cursor model 'glm-5.2-high' did not resolve to its exact picker row; the model was not switched
```
So: the server-side honesty fix from `f95026398` works (503, rollback), and the failure is squarely in `inject_model_command` / the row matcher.
`cursor-agent --list-models` on this machine prints **`glm-5.2-high - GLM 5.2`** and **`glm-5.2-max - GLM 5.2 Max`** — the model exists and is catalogued, so this is a resolution failure, not a missing model.
**Leading hypothesis (verify, do not assume):** the effort/variant machinery assumes a low/medium/high/xhigh ladder. GLM's variants are `high` and `max`, and the base display for `glm-5.2-high` is the bare "GLM 5.2" while `glm-5.2-max` is "GLM 5.2 Max" — so a family query for "GLM 5.2" matches two rows and the effort editor (`omnigent/cursor_native_bridge.py` ~1020-1047, `direction = "Down" if effort_index > cursor_index else "Up"`) may have no index for "max", or the highlighted row lands on the sibling and the (correctly strict) matcher rejects it instead of navigating to the right row. Note the code only ever *checks* the highlighted row and drives the effort editor; confirm whether it can *navigate the filtered list* to a different model row at all.
Relevant code: `omnigent/cursor_native_bridge.py` — `inject_model_command` (~790-960; backend-id attempt then family-query fallback at ~908-924, final check ~942), `_model_query_candidates` (~966), effort editor (~1020-1047), `_picker_highlighted_row` (~1075), `_picker_row_matches_display` (~1110); `omnigent/cursor_native.py:226-292` (variant/effort parsing, `_CURSOR_VARIANT_SUFFIX_RE`, default flagging at :235-259); runner entry `omnigent/runner/app.py:4436-4466`; server surface `omnigent/server/routes/_sessions/helpers.py:5175`.
For symptom 2, the prior loop's Evaluator already established the mechanism: cursor deltas are **pane-diff fragments**, so the concatenated preview is gappy/overlapping chrome (it measured joined-live 3193 chars vs a 4047-char final item, every fragment a substring of the final text), and the authoritative `external_conversation_item` replaces the preview at the end — which is exactly "wrong until it finishes, perfect after". The preview text itself is the defect, not the delivery.

## Mission
Make switching to any model `cursor-agent` catalogues actually land that exact model; make the web UI reflect the server's answer instead of an optimistic guess; and make the mid-stream preview show text that matches what the final message will say (or show less, but never wrong).

## Slices
1. **picker-any-model** (`omnigent/cursor_native_bridge.py`, `tests/test_cursor_native_bridge.py`) — resolve and select the requested model for the whole catalogue, not just the grok family: handle variant ladders that are not low/medium/high/xhigh (GLM's `high`/`max`, and audit `cursor-agent --list-models` for every other shape present on this machine), disambiguate a family query that matches several rows by **navigating the filtered list to the exact row** rather than only inspecting the highlight, and keep the strict exact-variant matching from `f95026398` (never accept a sibling or truncated row). Must still tolerate wrapped rows on a ~41-column pane and a list scrolled out of the viewport.
2. **picker-ui-truth** (`web/`) — when the model-change PATCH fails, the picker must revert to the model that is actually active and surface the error naming the model; it must never keep displaying a rejected selection. Check whether the UI can read the authoritative active model back (the session snapshot / `session.model` SSE event) rather than trusting local optimistic state.
3. **live-preview-fidelity** (`omnigent/cursor_native_stream.py` and/or `web/`) — the mid-stream preview must not display text that differs from what the finished message will contain. Preferred: improve the pane-diff reconstruction so the preview is a correct prefix of the final text (strip chrome, handle overwrite/redraw and wrapping, never emit fragments out of order). Acceptable alternative if fidelity cannot be guaranteed: render the preview in a visibly provisional way that cannot be mistaken for the answer, or fall back to showing nothing until the item lands — but a silently wrong transcript is not acceptable. Record the trade-off you chose and why.

## Verification floor
Test-first. Unit tests for slices 1 and 3 driven by REAL captured panes (a synthetic fixture is what let two previous loops ship broken — capture from a pane your own throwaway starts, including the composer `→` line and at least one wrapped row). Web tests for slice 2 through the real component/store path, not a reducer in isolation. Plus:
- **A real e2e that switches models on a throwaway pane** (port ≥ 18900): read the footer via `capture-pane -p`, switch to a model in a DIFFERENT family (e.g. Grok → GLM 5.2), assert the footer becomes exactly that model; then switch to a different effort within a family; then a bogus id → honest failure and no persisted override. Switching to a model that is already active proves nothing — always switch away from the current footer.
- **A real streaming e2e**: one top-level cursor turn whose answer takes >15 s; capture the SSE deltas and the final item, and assert the rendered preview text at every point is a prefix/subset consistent with the final text (no fragment that contradicts it). Report the worst-case divergence you observed before and after the fix.

## HARD isolation — read this twice
`~/.omnigent` and `~/.claude/projects` are READ-ONLY. **`~/.cursor` is READ-ONLY and must not change in any way.** A previous loop drove a throwaway cursor pane through `/model`, and cursor persisted that choice into the user's global `~/.cursor/cli-config.json`, silently changing their default model from Medium to Low — that must never happen again. Therefore: **every throwaway `cursor-agent` you launch must run with a scratch `HOME` (and any cursor config-dir env it honours) so it cannot write the user's cursor config**, and before finishing you must verify `~/.cursor/cli-config.json` is byte-identical to how you found it (record its sha256 at start and end in your evidence). If you cannot isolate cursor's config, do not run a live model switch — say so and fall back to fixture-driven proof.
Never touch the live server on :6767, `omni host`, or /home/alex/omnigent (shares .git — no git commands there). **Never type into, start, or kill any EXISTING tmux/cursor-agent session — the user is actively working in them**; `capture-pane -p` on an existing pane is the only permitted interaction, and any pane you drive must be one your own throwaway started and killed afterwards. Throwaway servers on port ≥ 18900 with scratch `OMNIGENT_DATA_DIR`/`OMNIGENT_CONFIG_HOME` and `OMNIGENT_PROCESS_LOG_FILE` unset, torn down. Never kill processes you did not start.
Preserve every existing commit (no amend/rebase/revert); new work = `slice(<slice-id>): …` commits with trailer `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`. Additive and env-overridable; CLAUDE.md rules (short scenario comments, no issue numbers in code). Tests need `PYTHONPATH=/home/alex/omnigent-cursor3`; pre-commit via `PATH=/home/alex/omnigent/.venv/bin:$PATH`.

## Acceptance
1. Live proof on a throwaway pane: switching Grok → **GLM 5.2** lands `GLM 5.2` exactly (not `GLM 5.2 Max`), and a within-family effort switch lands the exact variant; before/after `capture-pane` evidence for each.
2. A bogus id still fails honestly (no 200, no persisted override), and the shipped strict matching is not loosened to make GLM pass.
3. Web: a rejected model change leaves the picker showing the model that is actually active, with an error naming the attempted model — proven through the component/store path.
4. Streaming: the preview never displays text that contradicts the final message; the e2e reports the before/after divergence.
5. No regressions: the loop-cursor2 render/order fixes intact; claude-native and codex streaming unchanged; cursor sub-agents unchanged; `OMNIGENT_CURSOR_STREAM=0` still cleanly disables pane streaming.
6. Suites green: `cd web && npx vitest run` for touched tests; `uv run pytest -q tests/test_cursor_native_bridge.py tests/test_cursor_native_forwarder.py tests/test_cursor_native_stream.py tests/test_cursor_native_permissions.py tests/server/integration/test_sessions_model_override.py` plus touched tests; Trio-compat (`uv run pytest -q tests/tools/builtins/test_spawn.py tests/runner/test_runner_dispatch.py tests/server/integration/test_sessions_child_sessions.py -k 'reasoning_effort or session_create_spawns_child_under_caller or registered_native_agent_create_derives_launch_args_from_root_spec'`); pre-commit clean on changed files; slice commits present; tree clean except mailbox.
7. Isolation audit, stated explicitly in REPORT: `:6767/health` 200; **sha256 of `~/.cursor/cli-config.json` identical at start and end**; nothing written under `~/.omnigent` or `~/.claude/projects`; no `:189xx` listeners left; no leftover cursor-agent/tmux started by the loop; the user's existing panes untouched.
8. REPORT ends with operator steps: fast-forward `trio-v0.10.0-fixes`, `cd web && npm run build`, `omni server stop`, then switch to GLM 5.2 from the web picker and run one long cursor turn.
