# GOAL — Fix the four diagnosed production problems on top of the Trio patches, in an isolated worktree

Target repo: /home/alex/omnigent-fixes (git worktree, branch `trio-v0.10.0-fixes`, base dadaa04d5 = v0.10.0 + 3 Trio-compat commits + investigation mailbox). Mailbox: loop-fixes/. Diagnosis of record: `loop/REPORT.md` (read it; do not re-derive) with evidence under `loop/evidence/`.

## Mission
Implement, test, and independently verify fixes for the four root causes in loop/REPORT.md — SQLite pool starvation, harness processes not stopped on close/disconnect, fork oversize context, and slow CLI-output rendering — as small isolated commits on top of the existing Trio patches, without touching the user's live installation.

## Verification floor
Every slice: (1) unit/integration tests added or extended for the changed behavior, run with `uv run pytest -q <targeted paths>` and green; (2) `pre-commit run --files <changed files>` clean; (3) one behavioral demonstration captured under loop-fixes/evidence/iter<N>/<slice>/ (a script, log excerpt, or measurement proving the before/after — e.g. pool checkout under 20 concurrent session-open calls no longer waits 30 s; a child process tree is gone after close; oversize fork refused/compacted with the exact byte count; delta cadence improved). Evidence must be produced against a THROWAWAY server/data dir, never the live one.

## HARD isolation constraints (user's production setup)
- Never read/write `~/.omnigent`, `~/.claude/projects`, `~/.cursor`, or the running server on `http://127.0.0.1:6767`; never restart/kill `omni host`, the runner, or any process you did not start. Throwaway servers must use `OMNIGENT_HOME=<scratch>` (or the equivalent data-dir/env the code supports — verify in omnigent/cli.py / config), a free port ≥ 17000, and be torn down by the slice that started them.
- Work ONLY in /home/alex/omnigent-fixes. Never touch /home/alex/omnigent (the user's checkout of the same repo — it shares .git; do not switch its branch, stash, reset, or run git commands there).
- Preserve the existing commits (480b6eea9, 780962a5d, ead098caf Trio patches): never amend/rebase/revert them. New work = new commits only: `slice(<id>): …` + trailer `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.
- Keep each fix minimal and rebase-friendly vs upstream omnigent-ai/omnigent (open PRs #4976, #5603, #5405, #4913, #5081; merged #5544): prefer additive, env-overridable changes; do not restructure routes/stores.
- Follow CLAUDE.md: short scenario comments (no PR/issue numbers in code), named DB sessions, framework-instruction rules.

## Slices (in this order; each independently shippable)
1. **sqlite-pool** — `omnigent/db/utils.py:228-238` sqlite `create_engine` gets an explicit pool (mirror the Postgres block at :268-289 in spirit: sane `pool_size`/`max_overflow`, short `pool_timeout` ≤ 10 s, `pool_pre_ping`, and ensure WAL + `busy_timeout` are applied via the connect hook if not already). Env-overridable (`OMNIGENT_SQLITE_POOL_SIZE`, `..._MAX_OVERFLOW`, `..._POOL_TIMEOUT_S`). Test: concurrency test that opens > (old 5+10) concurrent checkouts and asserts no `TimeoutError`/QueuePool wait beyond the new timeout. Done: 20+ concurrent `to_thread` session-open-like calls against a temp sqlite DB complete without a 30 s wait; existing tests/db green.
2. **kill-on-close** — (a) `sys_session_close` (`omnigent/runner/tool_dispatch.py:4934-5005`) performs the same stop as the UI Stop button before/with the tombstone PATCH (reuse the existing stop path; must not affect sibling sessions); (b) `_claude_stop` (`omnigent/runner/native/interrupt.py:438-472`) cancels the auto-forwarder task like `_uniform_stop` (:396); (c) after `tmux kill-session` in `claude_native_bridge.py:3139-3174`, verify the pane/process group is gone and escalate to SIGKILL of the recorded pgid/pids with a bounded wait (`_proc.py` helpers), never touching our own pgid. Do NOT add kill-on-SSE-disconnect (design decision deferred; note in REPORT). Tests: unit tests with fake subprocess/tmux for each; done when a closed session's process tree is provably gone in a throwaway run.
3. **fork-oversize-guard** — On fork (`routes_core.py:1979-2200` and the runner launch paths `orchestration.py:6283-6400`, `claude_native.py` rebuild/clone at 1780-1843 / 4125-4202, `claude_sdk_executor.py:3052-3123`): compute the bytes/approx tokens the target harness would receive; if above a configurable threshold (`OMNIGENT_FORK_MAX_CONTEXT_BYTES`, default ~600 kB ≈ 150k tokens), do NOT launch blind — honor existing summary-only compaction markers when rebuilding (drop pre-summary records), and if still oversize return a clear 4xx/error to the UI naming the size and threshold instead of a "Prompt is too long" launch. Also raise the `limit: 1000` item fetch (`claude_native.py:4223`) to paginate fully. Tests for threshold, compaction honoring, pagination. Done: the ~3.9 MB synthetic session from loop/evidence/iter1/rc-fork/scratch/measure_fork_bytes.py is refused/compacted with an actionable message; a small session forks unchanged.
4. **render-latency** — (a) cursor-native: `omnigent/cursor_native_forwarder.py` `_DEFAULT_POLL_INTERVAL_S` 0.7 → adaptive (fast ~0.15 s while output is arriving, backing off to 0.7 s when idle), env-overridable; (b) claude-native: `claude_native_forwarder.py:84` 0.25 → 0.1 s with the same idle backoff, and batch the per-delta POSTs (:3962-3995, :4041-4062) into one POST per poll tick preserving order; (c) UI: `web/src/store/chatStore.ts` `tapLiveDeltas`/`applyLiveDelta` (:4292-4327, :4371-4393) coalesce deltas per animation frame (rAF) like the generic pump. Tests: python unit tests for backoff + batching; vitest for the store coalescing (check web/ for the test runner). Done: measured delta cadence in a throwaway run shows first-token and steady-state latency reduced vs the constants in loop/evidence/iter1/rc-render/TIMING.md.

## Acceptance
1. Four `slice(<id>): …` commits on `trio-v0.10.0-fixes`, each with tests, pre-commit clean, and an evidence dir; `git log dadaa04d5..HEAD` contains only those plus mailbox commits.
2. Full targeted suites green: tests/db, tests/runner, tests/tools/builtins, tests/server/integration/test_sessions_child_sessions.py, plus the Trio-compat command from the trio-omnigent skill: `uv run pytest -q tests/tools/builtins/test_spawn.py tests/runner/test_runner_dispatch.py tests/server/integration/test_sessions_child_sessions.py -k 'reasoning_effort or session_create_spawns_child_under_caller or registered_native_agent_create_derives_launch_args_from_root_spec'` (proves Trio patches still work).
3. Isolation audit in VERDICT: `~/.omnigent` mtime unchanged, port 6767 server untouched (health still up, PID unchanged), no stray processes left by the loop.
4. REPORT.md ends with exact operator steps to deploy the branch into the user's live install (what to rebuild/restart) and a rollback (`git checkout trio-v0.10.0`).
