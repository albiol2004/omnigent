# Hotfix 3 — fork route must measure what the runner will actually render

Repo `/home/alex/omnigent-fixes` (git WORKTREE, branch `hotfix-fork-estimate`, based on trio-v0.10.0-fixes 5a515ec89). HARD isolation: never touch ~/.omnigent, ~/.claude/projects, live :6767, `omni host`, /home/alex/omnigent (no git there). Throwaway only (port ≥ 17500, scratch OMNIGENT_DATA_DIR/OMNIGENT_CONFIG_HOME). Never kill processes you did not start.

## Production failure (measured on the live DB, read-only)
Source session: 496 items, `serialized_context_bytes(payload)` = **488,824**; `summary_only_items` = identical (no compaction items). Fork: 480 items = **473,191** bytes → `fork_session` (`routes_core.py:2210-2260`) sees < 600,000 and does NOT call `compact_fork_items`. Then the runner (hotfix 2 fallback) rebuilds the Claude JSONL via `claude_native._ensure_local_claude_resume_transcript(guard=True)` and gets **788,545 bytes** (≈1.67× the API-dict JSON: per-record uuid/parentUuid/timestamp/sessionId/cwd envelope + content-block wrapping) → 413 at `claude_native.py:4254`. The user's fork dies although compaction would have fixed it.

## Required change (smallest correct)
Make the server's fork decision use the SAME yardstick as the runner:
1. Preferred: find the pure rendering function the native rebuild uses to turn Omnigent items into Claude JSONL records (grep `def .*records|def _render|_items_to_records|jsonl` in `omnigent/claude_native.py` near 4100-4260 — use `sed -n`; the file is >1 MB, NEVER read it whole) and, if it is importable server-side without runner state, compute `jsonl_context_bytes(records)` for the fork payload in `fork_session` and use THAT as `actual_bytes` for `guard_fork_context` (keeping `summary_only_items` as the compacted candidate). Also honor the target harness: only Claude-family (claude-native / claude-sdk) needs the JSONL estimate; codex/pi have their own rebuilders — apply the same idea if a pure renderer exists, else fall back to (2).
2. Fallback if no pure renderer is usable: an inflation factor `OMNIGENT_FORK_JSONL_INFLATION` (default **1.8**) applied to the serialized bytes before the guard (estimate = bytes × factor), documented in a 2-line comment.
3. Either way: compaction must then run (the 473 kB payload must trigger `compact_fork_items`), the post-compaction guard uses the same estimate, and `OMNIGENT_FORK_COMPACT=0` still yields the 413.
4. Runner side: in the hotfix-2 fallback branch (`orchestration.py` ~3840-3860 "rebuilding from compacted items"), if the rebuild is still oversize for a FORK, keep the 413 but make the message say the server did not compact (helps future debugging). No other runner changes.

## Tests
`tests/server/routes/test_fork_compact.py` / `test_fork_oversize_guard.py`: a payload of ~470 kB API bytes whose JSONL estimate exceeds 600 kB triggers compaction (mock the summary LLM call) and the fork returns 201 with compacted items; a truly small payload is untouched; estimate helper unit-tested against a real `_ensure_local_claude_resume_transcript` rebuild of a synthetic conversation (the estimate must be within ±10% of the real file size, or exact if using the renderer). Run: `uv run pytest -q tests/server/routes/test_fork_compact.py tests/server/routes/test_fork_oversize_guard.py tests/runner/test_fork_clone_fallback.py tests/runner/test_fork_context_guard.py tests/runner/test_fork_resume_oversize_guard.py tests/fork_context`, Trio-compat (`uv run pytest -q tests/tools/builtins/test_spawn.py tests/runner/test_runner_dispatch.py tests/server/integration/test_sessions_child_sessions.py -k 'reasoning_effort or session_create_spawns_child_under_caller or registered_native_agent_create_derives_launch_args_from_root_spec'`), `PATH=.venv/bin:$PATH pre-commit run --files <changed>`. Outputs → `loop-fixes/evidence/hotfix3/`. Throwaway e2e: build a synthetic 496-item/~490 kB session on a throwaway server, POST /fork, assert 201 + compaction item present + rebuilt JSONL < 600 kB.

## Finish
Commit product+test files only: `slice(compact-on-fork): size the fork guard by the rendered transcript, not the raw item JSON` with trailer `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`. Never amend/rebase/push/stash/reset. Print a compressed summary with the measured estimate-vs-real ratio.
