schema: 1
iteration: 0
max_iterations: 4
status: ready
mission: Determine and fix why a same-family native fork still took the compaction path, and make POST /fork return immediately in every case with background compaction and a per-session preparing state that never blocks the whole app.
verdict: pending
