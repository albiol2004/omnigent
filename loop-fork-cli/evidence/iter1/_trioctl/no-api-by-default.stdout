🤖 Implemented the `no-api-by-default` slice without committing or pushing.

Changed:
- `omnigent/fork_compact_routing.py`
  - Key-backed candidates require `OMNIGENT_FORK_COMPACT_ALLOW_API=1`.
  - Disabled candidates log `skipped (api disabled)`.
  - Errors explain API keys were not used and include the chain.
- `tests/fork_context/test_fork_compact_model.py`
  - Added default-disabled coverage.
  - Opted existing API tests into `ALLOW_API=1`.
- Evidence written under `loop-fork-cli/evidence/iter1/no-api-by-default/`.

Verification:
- Focused pytest: `10 passed`.
- Ruff format: passed.
- Ruff check: passed.
- Routing-only Pyrefly: `0 errors`.
- `git diff --check`: passed.
- Full pre-commit remains blocked by missing optional imports and pre-existing
  hardcoded-model findings in `loop-fork-real/evidence/`.

Protected files were unchanged. The CLI fallback remains intentionally deferred to the other slice.
