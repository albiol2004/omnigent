# GOAL — compact-on-fork must actually work in production: real LLM summary, honest failures, no fake progress

Target repo: /home/alex/omnigent-fixes (git WORKTREE, branch `fork-compact-real`, based on trio-v0.10.0-fixes 48d3a5c03 which is checked out LIVE in /home/alex/omnigent). Mailbox: loop-fork-real/. Prior work: loop/REPORT.md (diagnosis), loop-compact/ (compact-on-fork 1c2a417e3/22793b2dd/3b045eaeb), loop-fixes/ hotfixes 54b9ca859, 7792bcf5f, 47cbe1a31, 4010cfcba.

## Production failure (live server log 2026-08-28 20:01, branch 48d3a5c03)
```
INFO fork_compact compact_fork_items | Fork compaction model resolved: source=source_override model=fable
POST /v1/sessions/e348…/fork → 413 Request Entity Too Large  (205 ms, then 36 ms on retry)
```
No exception logged. The source session (496 items, 489 kB API JSON, 668–788 kB rendered) is a claude-native session whose pinned model is the CLI alias `fable`. `routes_core.py` fork_session (~2210-2260) catches the compaction exception (`except Exception as compact_exc: _publish_compaction_failed(source_id); raise OmnigentError(str(exc), code=exc.code) from compact_exc`) — the real cause is discarded. 36 ms means no LLM call was attempted: most likely the summary client cannot be built for model `fable` (native-CLI alias with no provider prefix / no API route), or the agent has no `llm` config. Meanwhile the UI showed the "Summarizing history…" animation because `_publish_compaction_in_progress` fired before failure. User observation: "the session was disconnected so it's impossible the conversation got compacted" — correct: compaction here is SERVER-side and must not depend on the runner/harness being connected; it must use a server-reachable LLM (config.yaml has anthropic + openai API keys, providers claude/codex subscription).

## Mission
Make an oversize fork of a real claude-native session on this machine succeed end-to-end — server-side summary produced by an actually-callable model, fork launches with `--resume` on a compacted transcript — and make every failure honest: the real exception is logged at WARNING with the model/provider that was tried, the 413 message tells the user why compaction did not happen, and the UI never shows "Summarizing…" unless a summary call is in flight.

## Verification floor
Test-first. Unit tests for model resolution and error surfacing; an integration test of `fork_session` with a fake LLM client asserting the compacted 201; AND one REAL run on a throwaway server (scratch OMNIGENT_DATA_DIR/OMNIGENT_CONFIG_HOME; copy `~/.omnigent/config.yaml` READ-ONLY into the scratch config dir so the real anthropic/openai keys are available; never write to ~/.omnigent) forking a copy of the real oversize conversation: copy ONLY rows for conversation e34847899b7d47b3ad322948d4ea6002 (hex id; ids are stored as 16-byte blobs) and its `conversations`/`omnigent_conversation_metadata`/`conversation_labels` rows out of `~/.omnigent/chat.db` (read-only `?mode=ro` sqlite) into the scratch DB, then POST /fork with the source's real pinned model `fable` and record: resolved provider/model, summary length, bytes before/after rendered, HTTP status, and a fake-`claude` launch showing `--resume` on a < 600 kB transcript. A real LLM call is expected here (one summary); record its cost if visible.

## HARD isolation
Never touch ~/.omnigent (read-only copies only), ~/.claude/projects, live :6767, `omni host`, /home/alex/omnigent (shares .git; no git commands there). Throwaway server port ≥ 17700, torn down. Never kill processes you did not start. Preserve all commits; new work = `slice(fork-compact-real): …` commits with trailer `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`. Rebase-friendly, env-overridable, CLAUDE.md comment rules (no issue numbers).

## Slices
1. **compact-model-routing** — In `omnigent/fork_compact.py` (and whatever builds the summary LLM client — trace from `compact_fork_items` → `compaction.compact` → llm client): resolve the pinned model to a server-callable LLM. Rules: if the pinned model is a native-CLI alias (no `provider/` prefix, or a name only valid inside claude/codex/cursor CLIs — reuse existing alias tables in `omnigent/inner/*_native*` / `omnigent/llms` if any), map it via a small explicit table + provider config (claude aliases → `anthropic/<best available>` when an anthropic key exists; codex aliases → `openai/...`), else fall back to the agent spec llm, else to `OMNIGENT_FORK_COMPACT_MODEL`; if NO callable model exists, fail BEFORE publishing "in progress". Log at INFO the full chain tried and the final `provider/model`. Unit tests for each branch.
2. **honest-failure** — `routes_core.py` fork_session: log the swallowed compaction exception at WARNING with traceback; put the reason in the 413 body (`"…; compaction failed: <reason>"`) so the UI shows it; publish `_publish_compaction_in_progress` only after the client is built and the call is about to start; publish failed/completed exactly once. Tests.
3. **real-run-e2e** — the throwaway run described in the verification floor, artifacts under loop-fork-real/evidence/iter<N>/e2e/. Fix whatever it reveals (this slice has been missing in every prior loop: nothing was ever tested against the real session/model).

## Acceptance
1. Real e2e artifact: POST /fork on the copied real conversation with pinned model `fable` → 201, resolved `provider/model` logged, compaction item present, rendered transcript < 600 kB, fake-claude launch uses `--resume`, source rows byte-identical after.
2. Failure path: with API keys removed from the scratch config, the same POST → 413 whose message names why compaction failed, WARNING log has the traceback, and no `compaction_in_progress` event was published (SSE capture or publish-helper spy).
3. Suites green: tests/server/routes/test_fork_compact.py test_fork_oversize_guard.py, tests/fork_context, tests/runner/test_fork_clone_fallback.py test_fork_context_guard.py test_fork_resume_oversize_guard.py, plus Trio-compat (`uv run pytest -q tests/tools/builtins/test_spawn.py tests/runner/test_runner_dispatch.py tests/server/integration/test_sessions_child_sessions.py -k 'reasoning_effort or session_create_spawns_child_under_caller or registered_native_agent_create_derives_launch_args_from_root_spec'`); pre-commit clean on changed files; slice commits present; tree clean except mailbox.
4. Isolation audit: `:6767/health` 200, no writes to ~/.omnigent by the loop, no `:177xx` leftovers.
5. REPORT ends with operator steps: fast-forward `trio-v0.10.0-fixes`, `omni server stop` (systemd restarts it), re-fork session e348… and expect a summarized fork.
