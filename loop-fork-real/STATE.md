schema: 1
iteration: 1
max_iterations: 4
status: ship
mission: Make an oversize fork of a real claude-native session on this machine succeed end-to-end — server-side summary produced by an actually-callable model, fork launches with `--resume` on a compacted transcript — and make every failure honest: the real exception is logged at WARNING with the model/provider that was tried, the 413 message tells the user why compaction did not happen, and the UI never shows "Summarizing…" unless a summary call is in flight.
verdict: SHIP
eval:
last_run: 2026-08-28

